  name: Generar examen diario

on:
  workflow_dispatch:
    inputs:
      force:
        description: "Crear otro examen aunque ya exista uno de hoy"
        required: false
        default: false
        type: boolean
permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Instalar dependencias
        run: pip install -r requirements.txt

      - name: Generar examen
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: gpt-5-mini
        run: python scripts/generate_exam.py

      - name: Publicar cambios
        run: |
          git config user.name "rtve-exam-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/exams.json
          git diff --cached --quiet && exit 0
          git commit -m "Añadir examen diario"
          git push
